# This repository has no CI provider or workflows. The no-mistakes gate
# treats this declaration on the default branch as "all checks passed"
# for a zero-check repository; without it the gate's CI monitor waits
# for checks that can never register.
no_ci: true
